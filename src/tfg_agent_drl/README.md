# tfg_agent_drl — Placeholder DRL

Paquete reservado para una futura capa de **aprendizaje por refuerzo profundo (DRL)** en el TFG.

**Estado actual:** scaffolding ROS 2 sin nodos ni lógica implementada. El trabajo activo del TFG está en:

- [`tfg_planner_slm/`](../tfg_planner_slm/) — planificación por SLM
- [`slm_eval/`](../../slm_eval/) — evaluación offline de modelos de lenguaje
- [`panda_ws/`](../panda_ws/) — simulación, visión y control

Este paquete se mantiene para coherencia con el título del repositorio (`franka-slm`) y posible extensión futura.
